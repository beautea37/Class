Select * from users;
--컬럼 추가(컬럼명 reg_date, 타입은 date)

ALTER TABLE USERS reg_Date Date;

Select count (userid) from Users Where userid = 'asdf'

Select count (userid) from Users Where userid = 'asdf'

update users

Set reg_date = sysdate;
